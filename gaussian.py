#!/usr/bin/env python3
def g(x):
    g = (1/math.sqrt(2*math.pi))*(math.exp(-x**2/2))
    return g

def interval(f, a, b, dx):
    f = x
    N = (b-a)/dx
    intList = []
    
    
    
def integrate(i, dx):